import {NavLink} from "react-router-dom";
import React from "react";

const NotFoundPage = () => (
    <div>
        404
        <a href={'/'}>Go to Home</a>
    </div>
)

export default NotFoundPage
